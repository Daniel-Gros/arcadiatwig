{% extends 'base.html.twig' %}

{% block title %}Éditer un animal{% endblock %}

{% block body %}
    <h1 class="text-center">Éditer un animal</h1>

    {{ include('animal_crud/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
<div class="d-flex justify-content-center my-4 gap-5">
    <a class="btn button" href="{{ path('app_animal_crud_index') }}">retour à la liste</a>
    {{ include('animal_crud/_delete_form.html.twig') }}
</div>
{% endblock %}
