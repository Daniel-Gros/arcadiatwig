{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock %}

{% block body %}
    <h1>Avis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avi.id }}</td>
            </tr>
            <tr>
                <th>Pseudonyme</th>
                <td>{{ avi.pseudo }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ avi.message }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ avi.note }}</td>
            </tr>
            <tr>
                <th>Validation</th>
                <td>{{ avi.validation ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avis_crud_index') }}">retour à la liste</a>

    <a href="{{ path('app_avis_crud_edit', {'id': avi.id}) }}">éditer</a>

    {{ include('avis_crud/_delete_form.html.twig') }}
{% endblock %}
