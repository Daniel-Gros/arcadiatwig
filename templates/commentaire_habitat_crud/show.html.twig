{% extends 'base.html.twig' %}

{% block title %}CommentaireHabitat{% endblock %}

{% block body %}
    <h1>CommentaireHabitat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire_habitat.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ commentaire_habitat.message }}</td>
            </tr>
            <tr>
                <th>Improvement</th>
                <td>{{ commentaire_habitat.improvement ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_habitat_crud_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_habitat_crud_edit', {'id': commentaire_habitat.id}) }}">edit</a>

    {{ include('commentaire_habitat_crud/_delete_form.html.twig') }}
{% endblock %}
