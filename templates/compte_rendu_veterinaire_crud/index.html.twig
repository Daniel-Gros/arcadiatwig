{% extends 'base.html.twig' %}

{% block title %}CompteRenduVeterinaire index{% endblock %}

{% block body %}
    <h1>CompteRenduVeterinaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Message</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for compte_rendu_veterinaire in compte_rendu_veterinaires %}
            <tr>
                <td>{{ compte_rendu_veterinaire.id }}</td>
                <td>{{ compte_rendu_veterinaire.date ? compte_rendu_veterinaire.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ compte_rendu_veterinaire.message }}</td>
                <td>
                    <a href="{{ path('app_compte_rendu_veterinaire_crud_show', {'id': compte_rendu_veterinaire.id}) }}">show</a>
                    <a href="{{ path('app_compte_rendu_veterinaire_crud_edit', {'id': compte_rendu_veterinaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_compte_rendu_veterinaire_crud_new') }}">Create new</a>
{% endblock %}
