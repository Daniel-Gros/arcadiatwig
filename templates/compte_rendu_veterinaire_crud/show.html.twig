{% extends 'base.html.twig' %}

{% block title %}CompteRenduVeterinaire{% endblock %}

{% block body %}
    <h1>CompteRenduVeterinaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>N° d'identifcation (id)</th>
                <td>{{ compte_rendu_veterinaire.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ compte_rendu_veterinaire.date ? compte_rendu_veterinaire.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ compte_rendu_veterinaire.message }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('app_compte_rendu_veterinaire_crud_index') }}">Retour à la liste</a>

    <a class="btn btn-dark" href="{{ path('app_compte_rendu_veterinaire_crud_edit', {'id': compte_rendu_veterinaire.id}) }}">Éditer</a>

    {{ include('compte_rendu_veterinaire_crud/_delete_form.html.twig') }}
{% endblock %}
