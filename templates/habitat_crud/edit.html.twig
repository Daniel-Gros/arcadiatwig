{% extends 'base.html.twig' %}

{% block title %}Éditer Habitat{% endblock %}

{% block body %}
    <h1 class="text-center">Éditer Habitat</h1>

    {{ include('habitat_crud/_form.html.twig', {'button_label': 'Update'}) }}
<div class="d-flex justify-content-center my-4 gap-5">
    <a class="btn button" href="{{ path('app_habitat_crud_index') }}">retour à la liste</a>
    {{ include('habitat_crud/_delete_form.html.twig') }}
    </div>
{% endblock %}
