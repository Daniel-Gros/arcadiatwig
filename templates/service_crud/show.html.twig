{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h1>Service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ service.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ service.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_service_crud_index') }}">back to list</a>

    <a href="{{ path('app_service_crud_edit', {'id': service.id}) }}">edit</a>

    {{ include('service_crud/_delete_form.html.twig') }}
{% endblock %}
